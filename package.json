{
  "name": "plugin-links",
  "version": "0.2.0",
  "description": "Create markdown links from namespaced github repos.",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha",
    "build": "rm -rf ./dist && babel lib -d dist --stage 0",
    "prepublish": "npm run build"
  },
  "keywords": [
    "plugins"
  ],
  "author": "Cameron J Roe <cameronjroe@gmail.com> (http://cameronjroe.com/)",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.4.0"
  },
  "dependencies": {
    "axios": "^0.7.0",
    "lodash": "^3.10.1",
    "q": "^1.4.1"
  }
}
